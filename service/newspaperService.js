// const models = require('../models');

// const createNewspaper = async (newspaperData) => {
//     try {
//         const newspaper = await models.Newspaper.create(newspaperData);
//         return newspaper;
//     } catch (error) {
//         throw new Error('Failed to create newspaper');
//     }
// };

// module.exports = {
//     createNewspaper,
// };
